<% const tieneTitulo = typeof titulo !== 'undefined'; %>
<% const esUsuarioLocal = typeof esUsuario !== 'undefined'; %>
<% const esAdministradorLocal = typeof esAdministrador !== 'undefined'; %>


<!doctype html>
<html lang="es">
<head>
    <title><%= tieneTitulo ? titulo : 'Productos' %></title>
    <% include head.ejs %>
</head>
<!---->
<body>

<nav class="navbar navbar-expand-lg bg-light fixed-top" >
    <a class="navbar-brand" href="/" style="color: black">
        Tienda
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul   class="navbar-nav ml-auto">
            <% if (esAdministradorLocal){ %>
            <a class="nav-link" href="/usuario/listar"  role="button"
               aria-haspopup="true" aria-expanded="true">
                Usuarios
            </a>
            <%}%>

            <li class="nav-item dropdown">
                <% if (esUsuarioLocal){ %>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="/tienda/registrar">Tienda</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/producto/registrar">Producto</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/evento/registrar">Venta</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/logout">Logout</a>
                        </li>
                    </ul>
                </div>

                <%}else {%>
                <%if (!esAdministradorLocal){  %>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/login">Login</a>
                        </li>
                    </ul>
                </div>

                <% }else {%>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="/usuario/logout">Logout</a>
                        </li>
                    </ul>
                </div>

                <% }%>
                <%}%>
            </li>

        </ul>

    </div>
</nav>

